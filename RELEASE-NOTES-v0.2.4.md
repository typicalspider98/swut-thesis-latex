# SWUT Thesis LaTeX v0.2.4

本版本修复模板字体字号被自动缩小的问题。

## 修复内容

- 移除全局 `Scale=MatchLowercase` 字体缩放，统一使用 `Scale=1`。
- 使摘要标题、关键词、正文和页眉页脚按照学校 Word 模板规定的物理
  字号输出。
- 保留 Windows、macOS 和 TeX Live 回退字体分支，字体选择逻辑不变。

## 影响范围

修复后，同一字号的文字可能比 v0.2.3 略大，段落换行和总页数可能随之
变化。建议升级后重新编译全文，并检查封面、摘要、目录、图表和分页。
